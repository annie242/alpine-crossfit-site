{
  "name": "alpine-crossfit-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Alpine CrossFit static site built with Eleventy.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "watch": "eleventy --watch",
    "clean": "rm -rf _site"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
